name: 'CI actions'

on:
  pull_request_target:
    types: opened

jobs:
  check_challenge:
    runs-on: ubuntu-latest

    steps:
      - run: echo ${{ github.event.pull_request.number }}

      - run: echo run ${{ github.event.repository.name }}
      - run: echo ${{ github.ref }}
      - run: echo ${{ github.head_ref  }}

      - name: Print PR html
        run: echo ${{ github.event.pull_request.html_url  }}
        
      - name: Print PR number
        run: echo ${{ github.event.pull_request.number  }}


