name: 'CI actions'

on:
  pull_request:
    branches:
      - gh-action

jobs:
  challenge_check:
    runs-on: ubuntu-latest
    container: node

    steps:
#      - name: Branch checkout
#        uses: actions/checkout@v3

      - name: Install GH CLI
        uses: dev-hanz-ops/install-gh-cli-action@v0.1.0

      - run: echo ${{ github.event.pull_request.html_url }}
      - run: echo ${{ github.event.repository.name }}
      - run: echo ${ GITHUB_HEAD_REF }
      - run: echo ${ GITHUB_REF }

#      - run: git config --system --add safe.directory /__w/JavaUltimate2022/JavaUltimate2022
#
#      - name: Close Pull
#        run: gh pr close ${{ github.event.pull_request.number }}
#            --comment "Auto-closing PR as verification completed"
#            --delete-branch
#        env:
#          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

