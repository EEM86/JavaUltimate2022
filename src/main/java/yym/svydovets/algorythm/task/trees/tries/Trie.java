package yym.svydovets.algorythm.task.trees.tries;

import org.apache.commons.lang3.NotImplementedException;

/**
 * Trie node should have root element TrieNode.
 * TrieNode should have get(), put(), containsKey() methods
 */
public class Trie {

  /**
   * Trie() Initializes the trie object.
   */
  public Trie() {
    throw new NotImplementedException();
  }

  /**
   * Inserts the string word into the trie.
   * @param word String
   */
  public void insert(String word) {
    throw new NotImplementedException();
  }

  /**
   * Search for the word in the trie.
   * @param word String
   * @return true if the string word is in the trie (i.e., was inserted before), and false otherwise.
   */
  public boolean search(String word) {
    throw new NotImplementedException();
  }

  /**
   * Search for the prefix
   * @param prefix
   * @return Returns true if there is a previously inserted string word that has the
   * prefix, and false otherwise.
   */
  public boolean startsWith(String prefix) {
    throw new NotImplementedException();
  }
}
